#include "Matrix.h"

/*
template Matrix<int>;

template <class T>
std::ostream& operator<<(std::ostream& os, Matrix<T>& matr) {

	for (int i = 0; i < matr.m; ++i) {

		for (int j = 0; j < matr.n; ++j) {

			os << matr(i, j) << " ";
         }

		os << "\n";
    }

	return os;
}

template <class T>
T& Matrix<T>::operator()(int i, int j) {

	if (-1 < i && i < m && -1 < j && j < n) {

		return matrix[i * n + j];
    }
}

template <class T>
void Matrix<T>::resize(int _m, int _n) {
	matrix.resize(_m * _n);
	m = _m;
	n = _n;
}
*/





